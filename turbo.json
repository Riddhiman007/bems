{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"]
    }
  },
  "globalDependencies": ["tsconfig.json"],
  "globalDotEnv": [".env"]
}
